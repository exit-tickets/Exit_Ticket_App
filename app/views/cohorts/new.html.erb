<h1> Hey, <%=@instructor.first_name %>! Create a new cohort right here!</h1>

<%= form_tag("/cohorts", method: "post") do %>
	<%= hidden_field_tag "instructor_id", "#{@instructor.id}"%>
	<br>
	<%= label_tag :name_of_cohort %>
	<%= text_field_tag :name %>
	<br>
	<%= submit_tag "Create Cohort" %>
<% end %>


